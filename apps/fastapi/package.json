{
  "name": "fastapi-app",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "install": ".venv/bin/pip install -r requirements.txt",
    "dev": ".venv/bin/uvicorn main:app --reload --host 0.0.0.0 --port 8001"
  }
}